defmodule Quix.Repo.Migrations.RelateQuizAttemptsToUser do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:quiz_attempts) do
      add :user_id,
          references(:users,
            column: :id,
            name: "quiz_attempts_user_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:quiz_attempts, "quiz_attempts_user_id_fkey")

    alter table(:quiz_attempts) do
      remove :user_id
    end
  end
end
